<!doctype html>
<html lang='de'>
  <head>
    <title>Stadt Koeln - Open Data</title>
    {#
      https://github.com/vitejs/vite/issues/3924
      It seems that vite with twig sorts the stylesheets by name in the build process.
      For that reason, we need to prepend the main stylesheet with '@'
      to ensure it is always the first being loaded
    #}
    <link rel='stylesheet' href='/src/styles/@styles.scss'>

    {% set theme = theme is defined ? theme : 'light' %}
    <link rel='stylesheet' href='/src/styles/themes/{{ theme }}.css'>
  </head>
  <body class='min-vh-100 d-flex flex-column'>
    {% include 'partials::header.twig' %}

    <main class='container flex-grow-1'>
      {% block content %}{% endblock %}
    </main>

    {% include 'partials::footer.twig' %}
  </body>
</html>
